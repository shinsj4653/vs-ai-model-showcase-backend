<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="visang.showcase.aibackend.mapper.MemberMapper">
    <!-- GET /members -->
    <select id="getMembers" resultType="" fetchSize="1000">
        select tmi.member_no, ttci.q_idx, ttci.topic_nm, ttci.subject_cd
        from ai_model_showcase.tb_member_info tmi
        join ai_model_showcase.tb_prob_solve_result tpsr on tmi.member_no = tpsr.member_no
        left join ai_model_showcase.tb_topic_info ttci on tpsr.q_idx = ttci.q_idx;
    </select>
</mapper>